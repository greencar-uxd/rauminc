# 작업 지침

[출력 원칙]
- 결론부터. 서론·요약 반복·맺음말 인사 금지.
- 묻지 않은 내용 추가 설명 금지. 확장은 요청받을 때만.
- 만들기 전에 판단: 꼭 필요한가 → 이미 있는 걸 재사용 가능한가
  → 표준 기능으로 되는가 → 그래도 필요하면 최소한만.
- 예시는 1개면 충분. 같은 말 다르게 반복 금지.
- 검증·에러 처리·보안·접근성은 축약 대상에서 제외.

[검증 원칙]
- 팩트 / 추론 / 확인 필요를 구분해 표기.
- 외부 사실은 출처 명시. 근거 없으면 없다고 말할 것.
- 불확실하면 단정 대신 가능 시나리오로.

[진행 원칙]
- 매 단계 확인받지 말고 자율 진행. 막힐 때만 질문.

---

## 이 레포 고유 규칙

**구성 (라움아이앤씨 웹사이트 리뉴얼 시안)**
- 순수 정적 HTML 단일 파일 구조. 빌드 없음. 페이지별로 CSS/JS 인라인.
- 시안 3종: `a`(미니멀 화이트) / `b`(레드 포인트 #C8102E) / `c`(다크 프리미엄) + 각 서브 5페이지, `index.html`이 허브.
- `images/web/` = 페이지가 참조하는 JPG (`web/dark/`는 C안용 저조도 그레이딩), `images/src/` = 생성 원본 PNG.

**배포**
- GitHub Pages가 `main` 루트를 그대로 서빙. **main 커밋 = 즉시 공개 배포** — 커밋 전에 배포돼도 되는 상태인지 확인할 것.
- Public 레포이므로 커밋하는 모든 파일은 공개 문서다. 내부 작업 노트·핸드오프 문서는 커밋하지 않는다.
- Pages 배포가 "Deployment failed, try again later"로 실패하면 GitHub 측 일시 오류 — 잡 재실행, 그래도 큐에 고착되면 main에 빈 커밋으로 새 런을 트리거한다(실제 발생 이력 있음).
- 파일명(URL)을 바꾸지 않는다. Pages는 리네임 리다이렉트가 없어 클라이언트에 공유된 링크가 404가 된다(과거 레포 리네임으로 링크 무효화된 사례 있음).

**절대 커밋하면 안 되는 것**
- 문의 담당자 개인 연락처(이름·휴대폰·개인 이메일) — 내부 CRM 전용, 화면·문서 어디에도 금지.
- 매출·재무 수치 — 내부 참고용, 사이트 노출 금지.
- 커밋 전 `grep`으로 전수 스캔하는 습관 유지.

**콘텐츠 규율**
- 미확정 정보(전화·이메일·수치)는 `〇〇` placeholder — 지어내지 않는다.
- 공개 DB 출처의 잠정값에는 "※ 공개 기업정보 기준 — 클라이언트 확인 후 확정" 각주 유지.
- 우하단 DRAFT 배지는 시안 단계 동안 유지.
- "Ochang Entral Park" 등 프로젝트 표기는 발주처 확인 전 임의 수정 금지.

**품질 기준 (후퇴 금지)**
- `DESIGN-VALIDATION.md`가 검증·수정 이력의 기준 문서. 접근성 수정 반영 상태(폼 라벨·16px 입력·포커스 트랩·캐러셀 일시정지·`aria-current`·`noscript` 폴백 등)를 되돌리지 않는다.
- 외부 리소스는 C안의 Noto Serif KR(Google Fonts)만 허용 — 다른 CDN 추가 금지.
- 수정 후에는 Playwright로 모바일(390px) 가로 오버플로 0px·JS 에러 0건을 확인하고 커밋.

**반복해서 틀렸던 것**
- `aspect-ratio` + `min-height` 조합은 폭 전이(width transfer)로 모바일 오버플로를 만든다 → `max-width:100%` 병기.
- 고정 헤더 안의 버튼에 z-index를 줘도 헤더 스태킹 컨텍스트에 갇힌다 → 오버레이보다 헤더 자체를 올릴 것.
<!-- Claude가 실수할 때마다 여기에 한 줄씩 추가 -->
